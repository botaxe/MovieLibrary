<h2 class="page-header"><%= director.name %></h2>
<div class="btn-row">
  <a class="btn" href="/directors/<%= director.id %>/edit"><span>Edit</span></a>
  <%- include('../partials/deleteForm', { url: `/directors/${director.id}` }) %>
</div>
<% if (moviesByDirector.length > 0) { %>
<h2 class="page-header">Movies By Director</h2>
<%- include('../partials/movieGrid', { movies: moviesByDirector }) %> <% } %>
